<div class="absolute inset-0 overflow-hidden" aria-hidden="true">
  <!-- Grid pattern -->
  <div class="absolute inset-0 grid-pattern"></div>

  <!-- Crosshair beams from center -->
  <div class="beam beam-h"></div>
  <div class="beam beam-v"></div>

  <!-- Radial glow -->
  <div class="absolute inset-0 radial-glow"></div>
</div>

<style>
  .grid-pattern {
    background-image:
      linear-gradient(rgba(0, 212, 170, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 212, 170, 0.04) 1px, transparent 1px);
    background-size: 48px 48px;
  }
  .radial-glow {
    background: radial-gradient(ellipse 600px 400px at 50% 60%, rgba(0, 212, 170, 0.08), transparent);
  }
  .beam {
    position: absolute;
    animation: beam-pulse 4s ease-in-out infinite;
  }
  .beam-h {
    top: 50%;
    left: 0;
    right: 0;
    height: 1px;
    transform: translateY(-50%);
    background: linear-gradient(90deg, transparent, rgba(0, 212, 170, 0.15), transparent);
  }
  .beam-v {
    left: 50%;
    top: 0;
    bottom: 0;
    width: 1px;
    transform: translateX(-50%);
    background: linear-gradient(180deg, transparent, rgba(0, 212, 170, 0.15), transparent);
  }
  @keyframes beam-pulse {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 1; }
  }
</style>
